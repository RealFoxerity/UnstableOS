#ifndef _SYS_PARAM_H
#define _SYS_PARAM_H

// this is basically the older version of limits.h
// applications like bash require it sometimes
// not exactly sure what goes into this file, but this seems to work so far

#include <sys/types.h>
#include <limits.h>
#include <endian.h>
#include <signal.h>

#endif